package com.scra.mepi.rapid_react.commands;

import edu.wpi.first.wpilibj2.command.SequentialCommandGroup;
import edu.wpi.first.wpilibj2.command.WaitCommand;
import com.scra.mepi.rapid_react.subsystems.ClimberSubsystem;

public class AutoClimb extends SequentialCommandGroup{

    public AutoClimb(ClimberSubsystem climber){
        // new ClimberCommand(climber, -0.5),
        // new WaitCommand(0.1),
        // new ClimbPIDComand(climber, 1),
        // new WaitCommand(0.6),
        // new ClimbPIDComand(climber, 0),
        // new 
    }
    
}
